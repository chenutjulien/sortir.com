{% extends "base.html.twig" %}

{% block title %}
    {{ parent() }}
{% endblock %}
{% block main %}
    <br>

    <div class="container-fluid bgdiv">
        <h3>Enregistre ton super profil et deviens un supero-héros !!</h3>
        {{ form_start(editRegister) }}
        {{ form_widget(editRegister) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        {{ form_end(editRegister) }}
    </div>
{% endblock %}