{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block main %}
    <br>
<div class="container-fluid bgdiv">
    <h3>Detail du site</h3>
    <table class="table">
        <thead>
            <tr>
                <th>Nom du site</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <td>{{ site.name }}</td>
            <td><a class="btn btn-primary" href="{{ path('site_index') }}">Retour à la liste</a> /
                <a class="btn btn-success" href="{{ path('site_edit', {'id': site.id}) }}">Modifier</a></td>
        </tbody>

    </table>



    {{ include('site/_delete_form.html.twig') }}
</div>
{% endblock %}
