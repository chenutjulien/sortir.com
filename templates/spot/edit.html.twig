{% extends 'base.html.twig' %}

{% block title %}Editer un lieu{% endblock %}

{% block main %}
    <br>
    <div class="container-fluid bgdiv">
    <h1>Editer un lieu</h1>
        {% if is_granted('ROLE_ADMIN') %}
    {{ include('spot/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        {% endif %}
<br>
    <a class="btn btn-info" href="{{ path('spot_index') }}">Retour à la liste</a>
        {% if is_granted('ROLE_ADMIN') %}
    {{ include('spot/_delete_form.html.twig') }}
    {% endif %}
    </div>
{% endblock %}
