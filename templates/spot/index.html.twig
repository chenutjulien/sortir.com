{% extends 'base.html.twig' %}

{% block title %}Spot index{% endblock %}

{% block main %}
    <h1>Spot index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Street</th>
                <th>Latitude</th>
                <th>Longitude</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for spot in spots %}
            <tr>
                <td>{{ spot.id }}</td>
                <td>{{ spot.name }}</td>
                <td>{{ spot.street }}</td>
                <td>{{ spot.latitude }}</td>
                <td>{{ spot.longitude }}</td>
                <td>
                    <a href="{{ path('spot_show', {'id': spot.id}) }}">show</a>
                    <a href="{{ path('spot_edit', {'id': spot.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('spot_new') }}">Create new</a>
{% endblock %}
