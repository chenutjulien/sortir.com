{% extends 'base.html.twig' %}

{% block title %}Lieu{% endblock %}

{% block main %}
    <br>
    <div class="container-fluid bgdiv">
    <h1>Lieu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ spot.name }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ spot.street }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ spot.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ spot.longitude }}</td>
            </tr>
        </tbody>
    </table>

        <div><img src="https://maps.google.com/maps?q=24.197611,120.780512"></div>
    
        <a class="btn btn-info" href="{{ path('spot_index') }}">Retour à la liste</a>
        {% if is_granted('ROLE_ADMIN') %}
    <a class="btn btn-warning ml-3" href="{{ path('spot_edit', {'id': spot.id}) }}">Editer</a>
    {{ include('spot/_delete_form.html.twig') }}
        {% endif %}
    </div>

{% endblock %}
